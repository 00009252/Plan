extends layout 

block content 
    div.container 
        h1 Create note 

        if error 
            p.error Empty submission! 

        form.form(action="/create" method='POST')
            label Title 
            input.input(type="text" name="title")

            label Description
            textarea.input(type="text" name="description")

            button.btn Create note